{
  "name": "pacman-reimagined",
  "version": "1.0.0",
  "description": "A Pacman-inspired browser game with AI enemies",
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js' --no-notify",
    "start": "browser-sync start --server"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  },
  "keywords": ["game", "pacman", "agar", "browser-game"],
  "license": "MIT"
}
